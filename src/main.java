import gui.JFrames;

public class main {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // Entrance window
        JFrames main = new JFrames();
        main.createFrames();
    }
    
}
